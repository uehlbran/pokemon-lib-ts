---
"@pokemon-lib-ts/gen1": minor
---

Add opt-in badge stat boosts: `Gen1BadgeBoosts` interface, `applyGen1BadgeBoosts()` function, and `badgeBoosts` constructor option on `Gen1Ruleset`. Badge boosts multiply relevant stats by 9/8 (floor) per pret/pokered core.asm.
